package com.dailymotion.kinta.workflows.builtin.playstore

import com.github.ajalt.clikt.core.CliktCommand


object ProdPlayStore : CliktCommand(name = "prodPlayStore", help = "Deploy a previous beta version to production track") {

    override fun run() {
    }
}